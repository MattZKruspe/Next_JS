import { Inter, Lusitana, Arima } from 'next/font/google';
 
export const inter = Inter({ subsets: ['latin'] });
export const Lusi = Lusitana({ subsets: ['latin'], weight: '700' });
export const Ari = Arima ({ subsets: ['latin'], weight:'700'});
